import { ValidCity, ValidData } from "./app.js"

test("It should return true", () => {
    expect(ValidCity).toBeDefined();
});

test("It should return true", () => {
    expect(ValidData).toBeDefined();
});